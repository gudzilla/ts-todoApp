// DELETE ALL ?
import { createSlice } from "@reduxjs/toolkit";

const initialState: string | null = null;

const todoEditModeSlice = createSlice({
  name: "editModeId",
  initialState,
  reducers: {},
});
